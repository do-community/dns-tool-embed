{
  "name": "dns-tool-embed",
  "version": "1.0.0",
  "description": "Tiny embed version of the DO Community DNS Lookup tool",
  "source": "src/index.js",
  "main": "build/bundle.js",
  "scripts": {
    "build": "microbundle build",
    "watch": "microbundle watch",
    "test": "eslint 'src/**/*.js'",
    "test:fix": "npm run test -- --fix"
  },
  "author": "DigitalOcean",
  "license": "Apache-2.0",
  "devDependencies": {
    "eslint": "^7.2.0",
    "microbundle": "^0.12.0"
  },
  "dependencies": {
    "node-fetch": "^2.6.0"
  }
}
